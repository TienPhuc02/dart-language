greet(String name, String title) {
  return 'Hello $title $name!';
}

void main() {
  print(greet('Jane', 'Professor'));
}

/*
Hàm là một đơn vị mã có thể tái sử dụng, thực hiện một tác vụ và có thể trả về một giá trị.
Một hàm bao gồm chữ ký hàm và phần thân.
Một hàm có thể có 0 hoặc nhiều tham số.
Sử dụng void từ khóa làm kiểu trả về nếu hàm không trả về bất kỳ giá trị nào.
Sử dụng return câu lệnh để trả về một giá trị từ hàm.
 */
