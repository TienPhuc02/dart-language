void main() {
  double distanceMile = 1;
  const toKm = 1.609;
  double distanceKm = distanceMile * toKm;

  print('$distanceMile Mile =  $distanceKm Km');

  // error
  // toKm = 1.6;

  //error
  // const currentTime = DateTime.now();
  // print(currentTime);
  final currentTime = DateTime.now();
  print(currentTime);
}

/*
Hằng số là một định danh có giá trị không thay đổi.
Sử dụng consttừ khóa để xác định một hằng số.
 */
